/**
 * Legacy compatibility re-export for HTTP stream tap helper.
 */
export {
  tapReadableStream,
  type TapCaptured,
  type TapReadableStreamOptions,
} from '../instrumentations/common/http/stream-tap';
